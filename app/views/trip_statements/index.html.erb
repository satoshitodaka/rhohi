<% provide(:title, "出張旅費申請一覧") %>
<h1><%= @user.name %>が申請した旅費</h1>
<div class="statements_index">
  <h3>旅費申請<%= @created_statements.count %>件</h3>
  <% if @created_statements.any? %>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>出張日</th>
          <th>出張理由</th>
          <th>出張先</th>
          <th>承認ステータス</th>
          <th>詳細・編集</th>
          <th>削除</th>
        </tr>
      </thead>
      <tbody>
        <% @created_statements.each do |trip_statement| %>
          <%= render trip_statement %>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>該当する申請はありません。</p>
  <% end %>
</div>