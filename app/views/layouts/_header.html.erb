    <header>
      <nav>
        <% if user_signed_in? %>
          <strong><%= link_to current_user.name, home_about_path %>でログイン中</strong>
          <%= link_to '新規申請', new_trip_statement_path %>          
          <%= link_to '申請一覧', trip_statements_path %>
          <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to 'ログイン', new_user_session_path %>
        <% end %>
      </nav>
    </header>